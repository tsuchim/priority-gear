# PriorityGear requirements audit

Audit date: 2026-05-17

Repository state audited:

- `main` / `devel`: `8ef7ac592e6167d71ced285458db2bd8465ea069`
- Latest release: `v0.3.1`
- `v0.3.1` tag target: `3fa7e3dc6e6b423e07a64a20b0780cceba89f4cc`
- Commits after `v0.3.1`: documentation only (`README.md`, `README.ja.md`, `docs/installer.md`)

## Result

`v0.3.1` satisfies the current GitHub-distributed formal installer requirements. No product, installer, or release-artifact gap was found during this audit. A new release is not required for the post-tag documentation-only changes.

## Requirements matrix

| Original requirement | Current implementation evidence | Tests / verification evidence | Release evidence | Status | Required action |
| --- | --- | --- | --- | --- | --- |
| AutoGear-like process priority manager: periodically enumerates processes, applies registered priority rules, avoids repeated unnecessary apply calls, records failures clearly. | `src/PriorityGear.App/Runtime/MonitoringController.cs` owns the monitor tick loop, refreshes process snapshots, builds rule decisions, calls `IPriorityApplier.SetPriority`, skips unchanged priorities through `PriorityDecision.ShouldApply`, and records throttled failure log entries. `src/PriorityGear.Core/PriorityRuleEngine.cs` evaluates registered rules in order. | `tests/PriorityGear.App.Tests/MonitoringControllerTests.cs` covers applying on state change, skipping unchanged priority, and throttling repeated identical failures. `tests/PriorityGear.Core.Tests/PriorityRuleEngineTests.cs` covers disabled rules, first-match behavior, `ShouldApply`, and failure state preservation. | Included in `PriorityGear-v0.3.1-win-x64-installer.zip` through the GUI app payload required by `scripts/inspect-release-artifacts.ps1`. | satisfied | None. |
| Foreground-aware active priority: supports base priority and separate active priority for supported User Mode targets. | `src/PriorityGear.Core/PriorityRuleEngine.cs` chooses `ActivePriority` only when `ActiveModeEnabled` is true and the foreground process id matches; otherwise it uses `BasePriority`. `src/PriorityGear.App/MainWindow.xaml` exposes `Base`, `Active`, and `Active Mode` rule columns. `MainWindow.xaml.cs` wires `WindowsForegroundProcessSource` into `MonitoringController`. | `PriorityRuleEngineTests.Decide_UsesActivePriority_WhenActiveModeEnabledAndForeground`, `Decide_UsesBasePriority_WhenProcessIsNotForeground`, and `MonitoringControllerTests.Tick_ForegroundChangeChangesDesiredPriority` cover the base/active decision path. | GUI app payload is included in the `v0.3.1` installer artifact. | satisfied | None. |
| GUI configuration by ordinary user: rule configuration available from GUI; ordinary users configure allowed User Mode rules; GUI must not claim unsupported System Mode editing. | `src/PriorityGear.App/MainWindow.xaml` provides Add rule, Delete rule, Save rules, and editable user-rule grid columns. `MainWindow.xaml.cs` persists rules through `RuleStore` and updates the monitor controller. The System Mode GUI surface is a status button/text block and says mutating service commands are admin-only; no machine-rule editor is exposed. | `tests/PriorityGear.App.Tests/RuleStoreTests.cs` covers save/load, malformed JSON failure, atomic temp cleanup, and delete persistence. `tests/PriorityGear.App.Tests/SystemModeContractTests.cs` keeps machine scope separate from user rules and checks System Mode status formatting. | GUI app is included in the installer payload. The release notes and README describe System Mode status visibility and do not claim GUI machine-rule editing. | satisfied | None. |
| Privilege-aware operation: User Mode handles user-accessible processes without service install; System Mode uses LocalSystem service; scope depends on Windows access boundaries; denied targets are reported explicitly. | User Mode uses `WindowsPriorityApplier` directly from the GUI path and inspects `PriorityWriteLikelyPossible` before applying. System Mode is implemented by `PriorityGear.Service` and `PriorityGear.Setup` configures `PriorityGear.Service` as LocalSystem. `ServiceCommandHandler` separates status-pipe commands from admin commands and reports authorization data. | `MonitoringControllerTests` covers denied User Mode inspection and failure logging. `ServiceCommandHandlerTests` covers status pipe rejection of mutation/probe commands, admin authorization requirements, caller identity unavailable denial, and machine-rule safety failures. `InstallerStateMachineTests.NonLocalSystemAccountFails` covers service account enforcement. | `v0.3.1` release artifact contains `PriorityGear.Setup.exe` and service payload; installer docs state LocalSystem service and Windows-boundary risk. | satisfied | None. |
| System process support within Windows boundaries: System Mode can change safe LocalSystem-owned targets; service-name machine rules and service-process discovery exist; shared-host `svchost.exe` mutation is guarded and not enabled by default; arbitrary shared-host mutation is not claimed. | `MachineRuleMonitor` applies enabled, admin-approved machine rules and service-name rules. It uses `ServiceProcessDiscovery.FindByServiceName`, rejects shared-host service processes unless `AllowSharedServiceHost` is set, and rejects executable-only `svchost.exe` rules by default through matcher eligibility. | `ServiceCommandHandlerTests` covers approved machine-rule application path, unapproved/dry-run/executable-only `svchost.exe` rejection, service discovery request/metadata, and shared-host safety. Existing verification notes record safe temporary LocalSystem service testing. | `v0.3.1` release notes and README state arbitrary shared-host `svchost.exe` mutation remains out of scope. | satisfied | None. |
| Formal GitHub installer release: GitHub Release provides formal installer artifact; includes `PriorityGear.Setup.exe`; double-click + UAC install/update exists; installer configures LocalSystem service; preserves ProgramData rules/logs; uninstall/cleanup exists; artifact inspection passes. | `src/PriorityGear.Setup/Program.cs` is the installer entrypoint, requires elevation for install/uninstall, uses `InstallerStateMachine`, configures service account as LocalSystem, verifies status pipe, preserves ProgramData rules/logs, and supports `--uninstall`. `scripts/package-release.ps1` builds `PriorityGear-$tag-win-x64-installer.zip`. | `InstallerStateMachineTests` cover payload, service, status, account, identity, binPath, and cleanup behavior without admin rights. `scripts/inspect-release-artifacts.ps1` requires `PriorityGear.Setup.exe`, `setup-version.txt`, service, CLI, and app payload, validates checksum, and rejects forbidden entries. | Public release `v0.3.1` is not draft/prerelease and uploads `PriorityGear-v0.3.1-win-x64-installer.zip` plus `PriorityGear-v0.3.1-SHA256SUMS.txt`. Downloaded public assets passed `scripts/inspect-release-artifacts.ps1`; public zip SHA-256 is `45EDDA447AC4CB6AEDE2E1F8B3B3FE39AA09BE66C64981D4FF526BC2F0D6D1EC`. | satisfied | None. |
| Adversarial safety gate: installer state-machine tests cover failure modes; failed steps are not reported as success; artifact inspection rejects malformed packages; fallback success paths are not introduced. | `InstallerStateMachine` returns failure on required step exceptions or failed status checks and only treats old-version cleanup as warning-only after required success criteria pass. `docs/adversarial-testing.md` defines the gate. `inspect-release-artifacts.ps1` rejects unsafe tags, checksum mismatch, missing payload, `.git`, `src`, `tests`, and `*.Tests.dll`. | `InstallerStateMachineTests` cover payload missing, stop/start/configure/copy failures, status timeout, non-LocalSystem account, stale/hostile binPath, missing identity, warning-only cleanup, and immutable result snapshots. CI test suite currently passes 87 tests. | `v0.3.1` was built by the Release workflow after restore/build/test/package/inspect gates passed. | satisfied | None. |
| Distribution statement: current requirement is GitHub Release distribution; Store / winget / signing / MSI/MSIX are out of current scope; do not pretend Store distribution is done. | `README.md`, `README.ja.md`, `docs/installer.md`, and `docs/release-drafts/v0.3.1.md` describe GitHub Release installer distribution and explicitly state the installer is unsigned and not Store, winget, MSI, or MSIX packaging. | Workflow sanity checks verify the plain-semver release workflow and tag-specific release notes path. Documentation inspection found only historical `v0.3.0` references outside current-release wording. | `v0.3.1` is a normal GitHub Release with installer zip and checksum. No Store, winget, signing, MSI, or MSIX artifact was published. | satisfied | None. |

## Release decision

No release-affecting product, installer, packaging, or artifact behavior changed after `v0.3.1`. The only commits after the tag update documentation to point to `v0.3.1` as the current release. Therefore, no `v0.3.2` release is needed for this audit.
