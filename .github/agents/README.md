# Agents

This directory is reserved for repository-specific agent definitions.

Project rules live in the repository root `AGENTS.md`. Keep any future agent definitions aligned with the clean-room policy, User Mode v0.1 scope, and Japanese user-facing reporting requirement.
