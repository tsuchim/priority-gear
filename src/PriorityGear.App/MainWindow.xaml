<Window x:Class="PriorityGear.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriorityGear.App"
        mc:Ignorable="d"
        Title="PriorityGear" Height="720" Width="1120" MinHeight="620" MinWidth="980">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button x:Name="RefreshButton" Content="Refresh" Width="96" Margin="0,0,8,0" Click="RefreshButton_Click" />
                <Button x:Name="StartButton" Content="Start" Width="96" Margin="0,0,8,0" Click="StartButton_Click" />
                <Button x:Name="StopButton" Content="Stop" Width="96" Margin="0,0,8,0" Click="StopButton_Click" />
                <Button x:Name="AddRuleButton" Content="Add rule" Width="120" Click="AddRuleButton_Click" />
            </StackPanel>
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" TextAlignment="Right" />
        </DockPanel>

        <DataGrid x:Name="ProcessGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="80" />
                <DataGridTextColumn Header="Name" Binding="{Binding ExecutableName}" Width="180" />
                <DataGridTextColumn Header="Priority" Binding="{Binding CurrentPriority}" Width="120" />
                <DataGridTextColumn Header="Capability" Binding="{Binding Capability}" Width="180" />
                <DataGridTextColumn Header="Path" Binding="{Binding ExecutablePath}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="Rules" FontWeight="SemiBold" Margin="0,12,0,6" />

        <DataGrid x:Name="RuleGrid" Grid.Row="3" AutoGenerateColumns="False" SelectionMode="Single" CellEditEnding="RuleGrid_CellEditEnding">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="80" />
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="180" />
                <DataGridTextColumn Header="Match" Binding="{Binding Match.ExecutableName}" Width="180" IsReadOnly="True" />
                <DataGridComboBoxColumn Header="Base" SelectedItemBinding="{Binding BasePriority}" Width="140" />
                <DataGridComboBoxColumn Header="Active" SelectedItemBinding="{Binding ActivePriority}" Width="140" />
                <DataGridCheckBoxColumn Header="Active Mode" Binding="{Binding ActiveModeEnabled}" Width="110" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <ListBox x:Name="LogList" Grid.Row="4" Margin="0,12,0,0" />
    </Grid>
</Window>
